# Generated by Anaconda 34.25.7.6
# Generated by pykickstart v3.32
#version=RHEL9
# Use graphical install
text

%addon com_redhat_kdump --enable --reserve-mb='auto'

%end

# Keyboard layouts
keyboard --xlayouts='us'
# System language
lang en_US.UTF-8

# Register System

rhsm --organization="${ org }" --activation-key="${ key }"

%packages
@^server-product-environment

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.6.0
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --none --initlabel

# Intended system purpose
syspurpose --role="Red Hat Enterprise Linux Server" --sla="Self-Support" --usage="Development/Test"

timesource --ntp-server=100.99.99.250
# System timezone
timezone America/Denver --utc

#Root password
rootpw --lock
user --groups=wheel --name="${ setup_user }" --password="${ setup_password }" --gecos="github"

reboot --eject

%post

# Setup passwordless sudo authentication

echo "github ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/packer
chmod 440 /etc/sudoers.d/packer

%end
